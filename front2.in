* / % + - < <= > >= == != ! = && ||
() {} ; ,
for if else while do switch int float print

var34 while <= ! > || now
56 *= the for % () loop;
>= and && is the / - + != not =
,is {} * switch not

scanf, main, case, continue

var2 [] is here 6u
var ### cannot read
@ \';
